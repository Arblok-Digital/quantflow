node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.broker-secrets.json
.broker-vault-key
.guardrails.json
.keel-kill-switch.json
.paper-book.json
*.db
*.db-shm
*.db-wal
trading.db
.audit-signing-key
.auth-sessions.json

# OpenCode task artifacts
.oc-tasks/

# workflow butuh token scope 'workflow' � push manual aja kalau dibutuhkin
.github/workflows/

# artifact agent internal (plans, worktrees branch lain) — jangan masuk repo utama
.kilo/

# design mockup lokal (portfolio opsional) — tidak di-push
opendesign/

# audit opini auditor — status validasi mengikuti TODO.md
LOGIC_AUDIT*.md
LOGIC_REVIEW*.md

# generated: hasil scan solana-scout (regenerated tiap scan) + log runtime
solana-scout/output/
solana-scout/logs/
*.tsbuildinfo
