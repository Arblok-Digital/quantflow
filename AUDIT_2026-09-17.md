> **RIWAYAT BUKTI — BUKAN STATUS PROGRES AKTIF. Baca [TODO.md](./TODO.md).**
> Koreksi status 17 Sep 2026: klaim F9 “lima bug selesai” di bawah terlalu luas. Account sudah menghitung reserved margin tetapi snapshot/balance belum; partial full-close mengambil fill kedua; BE/atomicity masih punya celah. Test historis hanya membuktikan skenario yang dijalankan. Paper belum terverifikasi menyeluruh, live belum disetujui.
> Riwayat dipertahankan agar hasil uji dan koreksinya dapat ditelusuri, bukan dihapus seolah tidak pernah terjadi.


# Audit trading engine — validasi runtime

Tanggal: **17 September 2026 (UTC+07)**. Pengujian sekitar 18:22–18:29.

## Kesimpulan untuk operator

**Belum direkomendasikan untuk real money.** Ini bukan hanya kesimpulan dari membaca kode: lima masalah berhasil direproduksi menggunakan aplikasi backend asli, request HTTP, SQLite sungguhan, dan restart proses untuk skenario persistensi.

Tetapi tidak semua bagian rusak: lifecycle normal berhasil, termasuk satu transaksi paper dengan data pasar publik real. Tidak ada perbaikan kode aplikasi dalam pekerjaan ini. Satu-satunya file proyek yang ditambahkan oleh audit ini adalah laporan ini.

## Metode, keselamatan, dan batas bukti

- Salinan 176 file aplikasi/test/config dibuat di `C:\Users\ARBLOK\AppData\Local\Temp\quantflow-audit-ab88e992`.
- Tidak menyalin `.env`, vault broker, sesi pengguna, guardrail pengguna, atau database trading asli. Setiap skenario menggunakan database baru dalam direktori sandbox masing-masing.
- Mode dipaksa `paper`; credential exchange/AI dikosongkan. Tidak mengirim order exchange real. Tidak menghubungi API lokal aplikasi pengguna yang sedang berjalan.
- Node v24.15.0; SQLite bawaan Node; Vitest existing; HTTP via native fetch.
- Runner mengimpor Express app asli dan menjalankannya di port loopback acak. Login dan pemeriksaan status paper dilakukan sebelum skenario. Order, update, close, dan cancel melalui endpoint aplikasi asli.
- Engine, database, auth, dan guardrails bukan mock. Enam skenario memakai fixture exchange yang dipasang di memori oleh harness (harga/depth diketahui); satu skenario memakai jaringan exchange publik tanpa fixture.
- IPC harness hanya untuk observasi internal, mengatur market fixture/cache, memanggil satu pass bracket monitor asli, dan memasang pemicu kegagalan SQLite. Tidak menambah endpoint aplikasi.
- Scheduler bracket tidak berjalan otomatis; pass dipanggil secara terkendali. Startup memakai initPaperBook/initGuardrails dan app.listen; bukan full startServer/Vite/writer-lease startup.
- **Ini backend API E2E/integration, bukan browser/UI E2E, bukan uji live exchange, bukan uji semua kondisi produksi.** Tidak membuktikan profitabilitas strategi atau penyebab dominan loss akun pengguna.
- Pemeriksaan SHA256 terhadap 176 file salinan vs proyek setelah pengujian: **0 perbedaan isi**. Perubahan proyek yang sudah ada sebelum audit tidak diubah/direvert.

## Baseline

| Pemeriksaan | Hasil |
|---|---|
| `npm test -- --reporter=dot` di sandbox | 14 file test, **166/166 passed** |
| `npm run lint` (tsc --noEmit) | Tidak ada error |
| Custom runner, percobaan final | 7 skenario mencapai assertion yang direncanakan |

**Penting:** PASS pada skenario bernama `Reproduce ...` berarti BUG BERHASIL DITUNJUKKAN. Bukan berarti invariant bisnis lolos. Dari 7 skenario final: 2 lifecycle sehat lulus, 5 reproduksi menunjukkan kegagalan bisnis.

## Bukti hasil aktual

### 1. Lifecycle normal — LULUS (market terkontrol)

HTTP open LONG 1 unit di 100, leverage 10, fee masuk 0.04; exit di 101.

- Cash setelah open: 9989.96; equity: 9999.96.
- PnL bersih: **+0.92** setelah pembulatan; cash akhir **10000.92**.
- DB mencatat CLOSED.
- Setelah proses dimatikan dan dijalankan ulang dengan DB sama: cash **10000.92**, realized **+0.92**, posisi terbuka **0**.

Insight: rumus dasar normal tidak otomatis salah. Fee masuk tidak terbukti dipotong dua kali pada jalur ini.

### 2. BE manual — BUG TERVERIFIKASI

HTTP open LONG entry 100, lalu update `breakEven:true`. Stop yang dikembalikan API **99.92**. Market digerakkan ke 99.92 dan bracket monitor dijalankan.

- Posisi benar-benar CLOSED oleh engine.
- Realized PnL **-0.16**, bukan impas.
- Invariant yang gagal: exit pada harga BE asumsi seharusnya tidak menghasilkan net loss sebelum biaya tambahan yang belum dimodelkan.

Lokasi: [store.ts](c:/Users/ARBLOK/Documents/ai-trading-agent-engine/src/paperbook/store.ts#L423).
BE otomatis dan SHORT belum diuji dalam custom runner ini; kesamaan rumusnya tetap temuan review kode, bukan klaim E2E tambahan.

### 3. Pending limit menurunkan equity semu — BUG TERVERIFIKASI

Modal 10000. Limit buy 1 unit di 90, leverage 10; order NEW, belum ada posisi atau fill.

- Cash dan equity sebelum: **10000**.
- Saat pending: cash **9991**, equity **9991**, marginLocked **0**, openCount **0**.
- Setelah cancel via HTTP: cash/equity kembali **10000**.

Insight: cadangan margin 9 bukan kerugian, tetapi equity memperlakukannya seperti uang hilang.
Lokasi: [equity](c:/Users/ARBLOK/Documents/ai-trading-agent-engine/src/paperbook/store.ts#L369), [limit reservation](c:/Users/ARBLOK/Documents/ai-trading-agent-engine/src/paperbook/fill.ts#L495).

### 4. Full-close saat depth tidak cukup — BUG TERVERIFIKASI

Open 10 unit di 100. Saat close, orderbook fixture hanya menyediakan **2 unit** di 101.

- Respons close: CLOSED, quantity order **10**, posisi terbuka menjadi **0**.
- Fee exit hanya **0.0808** (fee untuk 2 unit), tetapi gross PnL dihitung untuk 10 unit.
- Realized dilaporkan **+9.52**.

Invariant gagal: quantity yang ditutup harus sesuai quantity yang terisi; sisa 8 unit tidak boleh hilang. Tidak harus selalu ditolak, tetapi partial execution harus tercatat konsisten.
Lokasi: [full close](c:/Users/ARBLOK/Documents/ai-trading-agent-engine/src/paperbook/fill.ts#L781).

### 5. Realized partial TP hilang setelah restart — BUG TERVERIFIKASI

Open 10 unit entry 100, SL 98. Pasang exit plan melalui HTTP: partial 50% di 1R. Gerakkan market ke 102, panggil monitor; quantity tersisa **5**.

| Nilai | Sebelum restart | Sesudah restart |
|---|---:|---:|
| Cash | 9959.40 | 9959.40 |
| Realized PnL | **+9.60** | **0** |
| Quantity tersisa | 5 | 5 |
| Unrealized | 10 | 0 |
| Equity | 10019.40 | 10009.40 |

Bug utama: realized +9.60 tidak dibangun ulang. Cash tidak hilang. Perbedaan unrealized/equity tambahan berasal dari mark di-reset ke entry saat rehydrate sebelum refresh; **bukan bukti kerugian pasar baru**. Jangan mencampur kedua efek.
Lokasi: [partial accounting](c:/Users/ARBLOK/Documents/ai-trading-agent-engine/src/paperbook/fill.ts#L999), [rehydrate realized](c:/Users/ARBLOK/Documents/ai-trading-agent-engine/src/paperbook/store.ts#L295).

### 6. Close gagal DB meninggalkan state berbeda — BUG TERVERIFIKASI

Pada DB sandbox dipasang trigger `BEFORE INSERT ON positions WHEN NEW.status='CLOSED'` yang melempar `AUDIT_INJECTED_WRITE_FAILURE`. Setelah open, close dipanggil lewat HTTP.

- HTTP **400**, reason **DB_TX_FAILED**.
- Memori aplikasi: posisi **CLOSED**.
- SQLite: posisi masih **OPEN**.

Invariant gagal: operasi gagal harus mengembalikan memori dan DB ke keadaan konsisten.

Transparansi pengujian: percobaan pertama memakai trigger BEFORE UPDATE, sehingga tidak terpicu karena aplikasi memakai INSERT OR REPLACE. Assertion percobaan pertama gagal. Harness diperbaiki ke BEFORE INSERT dan seluruh skenario diulang dengan DB baru. Tidak ada kode aplikasi yang diperbaiki. Hasil percobaan pertama tetap disimpan.
Lokasi: [mutasi dan transaksi close](c:/Users/ARBLOK/Documents/ai-trading-agent-engine/src/paperbook/fill.ts#L849).

### 7. Public real-feed lifecycle — LULUS pada sampel ini

Tanpa market fixture: HTTP market feed, open paper, close paper, cek SQLite/cash, matikan proses, jalankan ulang.

Hasil percobaan final:
- Feed source: **BINANCE_VISION**; currentPrice respons **76400.01**.
- Quantity **0.001309 BTC**.
- Entry fill **76395.55**, exit fill **76395.55**.
- Fee masuk **0.04**, keluar **0.04**.
- Expected net **-0.08**, reported net **-0.08**.
- Cash sebelum/sesudah restart **9999.92**; realized tetap **-0.08**.

Harga feed dan harga fill tidak harus sama karena waktu fetch/sumber fallback berbeda. Source BINANCE_VISION di sini label respons market-feed; bukan klaim semua fill berasal dari venue tersebut. Runner tidak menyimpan provenance lengkap jalur fill, sehingga ORDERBOOK vs TICKER untuk sampel ini tidak disertifikasi.

Insight: pada sampel ini minus memang dari fee round trip, bukan rumus PnL terbalik. Satu transaksi tidak membuktikan strategi kalah/menang atau feed selalu real.

## Hal yang belum tervalidasi end-to-end

Temuan berikut dari review kode sebelumnya **tidak dinaikkan menjadi temuan E2E terverifikasi**:
- Kegagalan SL/TP pada order LIVE, leverage/margin mode dan rekonsiliasi exchange live. Tidak diuji agar tidak menyentuh real money.
- Apakah seluruh jalur AI/autopilot menolak synthetic frontend data.
- Fallback all-sources-down, funding, spot/futures parity, real exchange partial fill, precision semua simbol.
- Akurasi urutan intrabar replay, BE otomatis/SHORT pada runtime custom, UI dan websocket berkelanjutan.
- Efek terhadap riwayat PnL pengguna, performa out-of-sample, profitabilitas strategi, ketahanan berjam-jam/hari.

Lima bug di atas cukup menjadi blocker akuntansi/reliabilitas, tetapi **belum membuktikan persentase loss pengguna yang disebabkan tiap bug**.

## Rekomendasi berdasarkan bukti

1. Tunda live: benarkan full/partial fill, persistensi partial realized, rollback state, BE dan reserved margin terlebih dahulu.
2. Jadikan invariant bisnis test regresi: bukan test yang sekadar menyetujui output sekarang. Setelah fix, skenario reproduksi bug harus berhenti mereproduksi kegagalan dan test kontrak benar harus lulus.
3. Ulangi lifecycle dengan LONG/SHORT, maker/taker, reconnect, restart saat posisi terbuka dan gagal tulis.
4. Setelah alat ukur benar, rekonsiliasi transaksi pengguna (gross, fee, net, equity); pisahkan MANUAL/AUTOPILOT/REPLAY.
5. Baru uji strategi pada data out-of-sample dan beberapa kondisi pasar. Jangan menaikkan leverage untuk menutupi minus.

## Bukti dan cara mengulang

Semua harness/log/DB percobaan berada **di luar codebase**:
`C:\Users\ARBLOK\AppData\Local\Temp\quantflow-audit-ab88e992`

- `audit-child.mts`: bootstrap aplikasi dan kontrol test-only di memori.
- `audit-runner.mjs`: request HTTP, assertions, restart proses, rangkuman JSON.
- `baseline-tests.log`, `typecheck.log`: hasil existing tests/typecheck.
- `audit-results.json`, `e2e-run.log`: hasil final.
- `audit-results-first-run.json`, `e2e-first-run.log`: percobaan awal yang memiliki kesalahan trigger test.
- `cases`: DB SQLite baru per skenario dan server logs; `cases-first-run`: percobaan awal.

Untuk reproduksi: di sandbox tersebut, arsipkan/rename direktori `cases` yang sudah ada ke nama baru terlebih dahulu agar test mulai dari database kosong; jalankan `node audit-runner.mjs`. Jangan menjalankannya di root proyek atau memakai DB pengguna. Koneksi public API diperlukan untuk skenario terakhir. Direktori TEMP tidak permanen; arsipkan sebelum sistem membersihkannya. Laporan ini menyimpan angka kunci meski TEMP kelak hilang.

SHA256 artefak final saat audit:
- child: `0AB692E601EFBA5D907F989551DABBC81014F4F24DA1DD43A030287B5BC89C96`
- runner: `64FB931C1F02606AD602B483E3ADCF0AD894B286FEC434210457DCE855F06FF9`
- results JSON: `A1DFA8B951F58A4881C0BF90C5BDD6315106C7DF00A3BB096D301AF34045D107`

**Tidak ada bug aplikasi yang diperbaiki dalam audit ini.** Tujuan pekerjaan adalah membuktikan perilaku, bukan mengubahnya.

---

# Perbaikan F9 (2026-09-17, sesudah audit)

## Status

Setelah bagian audit di atas selesai, **lima bug yang terbukti telah diperbaiki dan diverifikasi ulang**. Paragraf "tidak ada bug yang diperbaiki" hanya berlaku untuk hasil audit di atas; bagian ini mendokumentasikan perbaikan sesudahnya.

## Daftar perbaikan

| # | Bug | Perbaikan | File |
|---|---|---|---|
| 1 | BE manual & otomatis menaruh stop di sisi rugi (net −0.16) | Stop BE kini sadar-fee: `X = E×(1+f)/(1−f)` untuk LONG (di ATAS entry), kebalikannya untuk SHORT → net ≈ 0 | `src/paperbook/store.ts`, `src/paperbook/exitEngine.ts`, `src/server/routes/broker.ts` (jalur live) |
| 2 | Pending limit menurunkan equity semu | `equity = cash + marginLocked + reservedMargin + unrealized`; field baru `reservedMargin` | `src/paperbook/store.ts`, `src/paperbook/types.ts` |
| 3 | Full-close mengabaikan partial fill (qty 10 ditutup saat depth 2) | Close kini depth-aware: hanya `fill.filledQty` yang dibukukan; sisa qty tetap OPEN lewat jalur partial | `src/paperbook/fill.ts` |
| 4 | Realized partial TP hilang setelah restart | `realizedPnlUSD` posisi kini kumulatif per eksekusi, tersimpan di DB (OPEN ikut), direkonstruksi dari `SUM(realized_pnl_usd)` semua posisi | `src/paperbook/fill.ts`, `src/paperbook/store.ts` |
| 5 | Close gagal DB: memori CLOSED, SQLite OPEN | Snapshot in-memory sebelum mutasi + rollback memori penuh di catch, paritas fail-closed dengan open | `src/paperbook/fill.ts` |

Catatan kontrak test: test BE lama mengunci rumus salah (mengimpor `BE_OFFSET_PCT_DEFAULT`, mengharapkan stop di bawah entry). Test diperbarui ke kontrak baru yang benar (BE di sisi profit, net=0) — perbaikan kontrak, bukan penyesuaian agar lulus.

## Verifikasi setelah perbaikan

| Verifikasi | Hasil |
|---|---|
| `tsc --noEmit` | Bersih |
| Suite penuh (final, setelah semua perbaikan) | **166/166 lulus** (termasuk 22/22 paperbook kontrak BE baru) |
| E2E FIX-1: BE manual → stop **100.080032** (di atas entry), net **0** | PASS |
| E2E FIX-2: limit pending → equity **10000** utuh, `reservedMargin` 9; cancel → cash kembali | PASS |
| E2E FIX-3 rollback: close gagal DB → memori **OPEN**, SQLite **OPEN**, cash 9989.96, posisi ada setelah restart | PASS |
| Regresi partial-PnL (2× restart): realized 9.6 bertahan, total 24.19 tanpa dobel | PASS |
| Regresi depth-aware: 2/10 tertutup, sisa 8 OPEN, total 9.196, bertahan restart | PASS |

Angka kunci: BE net **−0.16 → 0**; equity pending limit **9991 → 10000**; full-close **10 salah tutup → 2 tertutup + 8 tetap terbuka**; realized partial setelah restart **0 → 9.6**; rollback memori **CLOSED → OPEN (sinkron DB)**.

## Batas setelah perbaikan

- SL/TP order LIVE yang gagal dipasang tetap *best-effort* (audit awal): **belum diubah** — perbaikan yang benar butuh verifikasi akun live/testnet, di luar model keuangan paper.
- BE ≈ net 0 hanya pada fill tepat di harga stop; slippage/gap/funding riil tetap bisa membuat net negatif.
- Mark posisi saat rehydrate masih mulai dari entry sebelum refresh mark berikutnya (efek tampilan sementara unrealized, bukan kas).
- Verifikasi tetap backend paper; tidak ada pengujian live exchange atau UI.

---

## Pembaruan 17 September 2026 — kejujuran input prompt AI (19:32 UTC+07)

### Perubahan yang selesai

- `/api/ai-decision`: default confluence 75%, liquidity 14M/18M, RSI 50, EMA=harga dan imbalance 1.0 saat data absen diganti `No data`. Formatter angka menolak non-number/non-finite; angka nol tidak disamakan dengan data hilang.
- Label volume BSL/SSL kini menyebut **estimasi resting depth dari client, provenance belum diverifikasi**, bukan volume stop/likuidasi yang diukur dan bukan klaim otomatis data real.
- `/api/ai-decision` dan `/api/ai-advisor`: netflow, MVRV, SOPR, whale alerts dan bias on-chain dari facade saat ini dikeluarkan dari prompt, termasuk varian real-anchored. Anchor network real tidak memvalidasi analitik turunannya. Label client `source=REAL` tidak membypass kebijakan ini.
- Advisor mengembalikan `onChainEcho: null`, dataHealth onchain tidak OK, serta fallback insight menjelaskan analitik belum terverifikasi. Endpoint/panel telemetry blockchain tidak dihapus. Belum menambahkan provider analitik on-chain baru.

### Test dan batas klaim

Test permanen: `tests/integration/aiPrompt.test.ts`. HTTP memanggil route asli dan menangkap `contents` tepat sebelum SDK AI. Satu test menjalankan tiga payload (kosong, generator sintetis, generator real-anchored) pada kedua endpoint; memeriksa tidak ada angka fallback palsu/whale fiksi dan kebijakan bobot on-chain nol. Advisor sengaja menerima respons SDK yang tidak valid agar fallback respons ikut diuji. Auth, SDK, DB, Keel dan upstream dimock: **bukan uji kecerdasan model atau live exchange**.

- Sebelum filter decision terhubung: test gagal (RED).
- Setelah terhubung dan mencakup advisor: targeted test PASS, exit 0; diulang PASS.
- Full suite: **167/167 tests, 15/15 files PASS**, exit 0.
- TypeScript `tsc --noEmit`: PASS, exit 0.
- Sandbox baru: `C:\Users\ARBLOK\AppData\Local\Temp\quantflow-prompt-74c9d6b5`; log `prompt-red.log`, `both-prompts.log`, `requested-recheck.log`, `full-final.log`, `lint-both.log`.

### Koreksi audit wiring (menggantikan pernyataan chat sebelumnya)

1. `getOnChainAnalysis` **bukan dead code**: `fetchOnChainMetrics -> generateOnChainMetrics -> getOnChainAnalysis`. Varian real-anchored juga menghasilkan proyeksi, bukan pengukuran netflow/MVRV/SOPR/whale.
2. `liquidity-mapper` **aktif**: `runKeelQuantEngine -> generateSignal -> mapLiquidity`. `extractOrderFlow` juga punya pemanggil `buildFeaturePack`; keterhubungan feature pack tersebut ke keputusan web belum dibuktikan dalam pekerjaan ini.
3. Nama `hasRealDepth` di adapter hanya memeriksa bids/asks tidak kosong. Venue dilabeli BINANCE_SPOT dan timestamp dibuat saat normalisasi. Ini **belum verifikasi provenance/freshness** market data; tidak diperbaiki di pekerjaan prompt ini.
4. Perbandingan dua snapshot wall **tidak membuktikan “removed without fills”**, apalagi spoofing atau niat institusi. Klaim chat sebelumnya terlalu kuat.
5. Book depth bukan ukuran stop cluster atau liquidation volume. Zona swing tetap hipotesis struktur harga; bukan bukti edge atau praktik institusional.

### Sisa batas

Kebijakan ini berlaku untuk dua prompt dan echo advisor, bukan sertifikasi seluruh pipeline real-only. Generator untuk visualisasi tetap ada; sanitasi seluruh input teknikal/liquidity/provenance dan validasi live/testnet masih pekerjaan terpisah. Filter prompt tidak menjamin model selalu patuh atau strategi profit. Temuan financial model sebelumnya tidak diuji ulang secara menyeluruh oleh test prompt ini.

